type QueryPatient {
    patientById(id: ID): Patient
}

type Patient {
    id: ID

    firstname: String
    lastname: String
    patronymic: String

    birthday: Date
    avatarUrl: String

    clinic: Clinic
    email: String
    phone: String
}

